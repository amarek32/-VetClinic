@{
    ViewBag.Title = "Strona główna";
}

<h2>Panel zarządzania kliniką</h2>

<div class="row" style="margin-top: 20px;">
    <div class="col-md-3">
        <div class="card text-white bg-primary mb-3">
            <div class="card-header">Klienci</div>
            <div class="card-body">
                <h5 class="card-title">@ViewBag.LiczbaKlientow</h5>
                <a href="@Url.Action("Index", "Klients")" class="btn btn-light">Przejdź</a>
            </div>
        </div>
    </div>

    <div class="col-md-3">
        <div class="card text-white bg-success mb-3">
            <div class="card-header">Zwierzęta</div>
            <div class="card-body">
                <h5 class="card-title">@ViewBag.LiczbaZwierzat</h5>
                <a href="@Url.Action("Index", "Zwierzes")" class="btn btn-light">Przejdź</a>
            </div>
        </div>
    </div>

    <div class="col-md-3">
        <div class="card text-white bg-warning mb-3">
            <div class="card-header">Wizyty</div>
            <div class="card-body">
                <h5 class="card-title">@ViewBag.LiczbaWizyt</h5>
                <a href="@Url.Action("Index", "Wizytas")" class="btn btn-light">Przejdź</a>
            </div>
        </div>
    </div>

    <div class="col-md-3">
        <div class="card text-white bg-info mb-3">
            <div class="card-header">Leczenia</div>
            <div class="card-body">
                <h5 class="card-title">@ViewBag.LiczbaLeczen</h5>
                <a href="@Url.Action("Index", "Leczenies")" class="btn btn-light">Przejdź</a>
            </div>
        </div>
    </div>

    <div class="col-md-3">
        <div class="card text-white bg-secondary mb-3">
            <div class="card-header">Weterynarze</div>
            <div class="card-body">
                <h5 class="card-title">@ViewBag.LiczbaWeterynarzy</h5>
                <a href="@Url.Action("Index", "Weterynarzs")" class="btn btn-light">Przejdź</a>
            </div>
        </div>
    </div>
</div>
