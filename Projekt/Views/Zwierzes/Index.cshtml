@model IEnumerable<Projekt.Models.Zwierze>

@{
    ViewBag.Title = "Zwierzęta";
}

<h2>Lista zwierząt</h2>

<p>
    @Html.ActionLink("Dodaj nowe zwierzę", "Create")
</p>

<table class="table">
    <tr>
        <th>@Html.DisplayNameFor(model => model.Klient.Imie)</th>
        <th>@Html.DisplayNameFor(model => model.Imie)</th>
        <th>@Html.DisplayNameFor(model => model.Gatunek)</th>
        <th>@Html.DisplayNameFor(model => model.Rasa)</th>
        <th>@Html.DisplayNameFor(model => model.DataUrodzenia)</th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>@Html.DisplayFor(modelItem => item.Klient.Imie)</td>
            <td>@Html.DisplayFor(modelItem => item.Imie)</td>
            <td>@Html.DisplayFor(modelItem => item.Gatunek)</td>
            <td>@Html.DisplayFor(modelItem => item.Rasa)</td>
            <td>@Html.DisplayFor(modelItem => item.DataUrodzenia)</td>
            <td>
                @Html.ActionLink("Edytuj", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Szczegóły", "Details", new { id = item.Id }) |
                @Html.ActionLink("Usuń", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
